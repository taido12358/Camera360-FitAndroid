# Release Workflow

No release process exists yet for either app (no signed release build, no
store listing, no CI pipeline). This file should be filled in once one is
defined — see rules/build-release/README.md for the build-time mechanics
that any future release process will build on (signing, `assembleRelease`,
etc.).

## Status

PLANNED
