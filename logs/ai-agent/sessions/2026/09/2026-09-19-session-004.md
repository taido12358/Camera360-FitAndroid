# Session 004 — 2026-09-19

## Goal
/goal: work autonomously (code, fix, test on emulator; ask ChatGPT/Gemini via
Chrome if stuck; commit + push per task).

## Done
- First real build: `assembleDebug` succeeds; installed and ran on the
  `Medium_Phone_API_36.1` emulator (virtual scene camera).
- Found and fixed via emulator testing (TASK-003 was previously unverified):
  1. `GyroscopeManager` used `getOrientation()` pitch (-90° = camera at
     horizon) while the rest of the app treats pitch as elevation; also
     degenerate azimuth when upright. Now uses camera-forward vector.
  2. `StitchingEngine` mixed ENU (x=E,y=N,z=Up) basis vectors with its own
     (E,Up,N) direction vectors → panorama vertical axis would map to
     North. Basis components now swapped.
- Uncommitted work from the prior session (no-gyroscope guard) committed too.
- Verified on emulator: with camera pose north/horizon, auto-capture fired
  and marked the middle-row frame; dots laid out correctly.

## Testing tip
Emulator pose: script sets accelerometer + magnetometer (see
rules/android-native/sensors-gyroscope.md).

## Not done
Full 24-frame capture + stitch check on emulator/device.
