# Session 005 — 2026-09-20

Continuation of the /goal autonomous run (commit + push per task).

## Findings
- Real test phone = Galaxy A12 SM-A127F: accelerometer only (no gyro, no mag,
  no rotation vector). The sensor pipeline cannot run on it. OpenCV's Android
  Maven artifact has no Java stitching/features2d/calib3d, so no OpenCV Stitcher.

## Done
- Gain (exposure/WB) compensation across overlaps (+6 tests).
- Accelerometer-only pose reconstruction: `PoseMath.rotationFromGravity`,
  `YawRegistration` (NCC + robust graph solve), `GravityManager`, manual-mode
  capture/stitch UI in CaptureScreen/CaptureViewModel (+12 tests, 26 total).
- Smoke-tested on the real phone: manual UI renders, shots counted, stitch runs
  and reports a clear error on featureless dark photos (no crash).

## Not verified
- Real overlapping photos on the phone (needs the user to rotate the phone).
- Gain compensation on real overlapping data.
