# CI/CD Rules

No CI/CD pipeline exists (`.github/workflows` not present, no other CI
config found). This is a real gap, not a deliberate simplification.

## Status

PLANNED — when a pipeline is added, document it here and in
docs/workflow/release.md, and record the choice (GitHub Actions vs.
other) as an ADR if it's non-obvious (e.g. needs macOS runners for iOS,
Android SDK setup for Camera360).
